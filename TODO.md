# TODO

- [ ] Create TypeScript types for ImportPage summary, tasks, shots, sequences to remove implicit `any` errors.
- [ ] Update `src/app/import/page.tsx` to use typed `summary` state and typed callback params for `map`/`find`.
- [ ] Run TypeScript/Next build or lint to confirm TS7006 errors are resolved.

